<h1>Friends</h1>

MY LOCATION IS CURRENTLY:

<%= @user.location if @user.location %>


<%# form_tag ('/index', method: "patch") do %>
  <% @friends.each do |friend| %><br><br>
    <img src="<%= friend.pic %>"><br>
  <%#= label_tag(friend.name) %><br>
    <% if friend.id  %>
      <%= "Following" %>
    <% else %>
      <%= "Click to Follow!" %>
    <% end %>
    <br>
    <% if friend.location %>
      <%= friend.location  %>
    <% end %>
  <% end %>
<%# end %>



<% form_tag ('/users', method: "patch") do %>
  <% @friends.each do |friend| %><br><br>
    <img src="<%= friend.pic %>"><br>
  <%= label_tag(friend.name) %><br>
    <%= if friend.id ? "Following" : "Click to Follow!" %>
    <br>
    <%= friend.location if friend.location %>
  <% submit_tag %>
  <% end %>
<% end %>


<!-- HERE IS HOW WE GOT GEO ENABLED FROM THE FRIENDS HASH IN THE RAILS CONSOLE:
 client.friends.first.attrs[:geo_enabled] -->
 
<!-- CAN ALSO USE THIS
 client.friends.first.attrs.geo_enabled? -->


